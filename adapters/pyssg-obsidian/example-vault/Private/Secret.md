---
title: Secret
publish: false
---

This note is marked `publish: false`, so it is never rendered into the site.
It demonstrates opting a note out under the default denylist (publish everything
except notes you explicitly hide).
